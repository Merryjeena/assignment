#include <stdio.h>
main()
{
	 int sum=0,diff=0,a,b,c,d;
	printf("Enter the nos to be added: ");
	scanf("%d%d",&a,&b);
	sum=a+b;
	printf("Sum= ",sum);
	 printf("Enter the nos to be substracted: ");
        scanf("%d%d",&c,&d);
        diff=c-d;
        printf("Difference= ",diff);
	return 0
}
edited
